version: '2'

services:
  #db:
   # image: postgres
    #environment:
    # PROYECTOGPS_DATABASE_USERNAME: 'pguser'
    # PROYECTOGPS_DATABASE_PASSWORD: 'lina123*'
    #volumes:
     # - /tmp/postgres:/var/lib/postgresql/data
    #ports:
     # - "5432:5432"
  railsapp:
    build: .
    command: rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/railsapp
    ports:
      - "3000:3000"
    #depends_on:
      #- db
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - railsapp
